// CUSTOM FUNCTION

// 🔥🔥🔥🔥🔥🔥You're writing func(string) error everywhere. If the signature changes, you update it in 10 places.🔥🔥🔥🔥🔥🔥

// func doSomething(action func(string) error) { ... }
// func doAnother(action func(string) error)   { ... }
// func doMore(action func(string) error)      { ... }

package main

import (
	"errors"
	"fmt"
)

type NameHandler func(name string) error

func doSomething(action NameHandler) {
	err := action("Ram")
	if err != nil {
		fmt.Println("error:", err)
	}
}

func main() {

	greet := NameHandler(func(name string) error {
		fmt.Println("Hello", name)
		return nil
	})

	fail := NameHandler(func(name string) error {
		return errors.New("something went wrong")
	})

	doSomething(greet)
	doSomething(fail)
}
