// This is where it gets powerful:

package main

import "fmt"

func greeter(prefix string) func(name string) string {
	return func(name string) string {
		return prefix + " " + name
	}
}

func main() {

	hello := greeter("Hello")

	returnedValu := hello("Ram")

	fmt.Println(returnedValu)

}
